{{{navbar}}}

{{#each users}}
    {{{this}}}
{{/each}}

{{{test}}}
<div class="content-grid">
    {{#each pins}}
        <div class="content-grid__item">
            {{{this}}}
        </div>
    {{/each}}
</div>
{{{popup}}}
