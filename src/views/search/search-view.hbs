{{{ navbar }}}

<div class="search__container">
    {{{ input }}}
    {{{ btn }}}
</div>

<div class="content-grid">
    {{#each pins}}
        <div class="content-grid__item">
            {{{ this }}}
        </div>
    {{/each}}
</div>